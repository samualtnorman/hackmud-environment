function (context, args) {
	// Because this script is in a folder, it's only pushed to the user "my_user"
	// Rename the folder to the name of one of your users and this script will be pushed to only that user
}
